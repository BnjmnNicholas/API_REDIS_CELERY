# FastAPI and server dependencies
fastapi==0.104.1
uvicorn==0.24.0

# Celery for task queuing
celery==5.3.4

# Redis client (async support for FastAPI)
redis==5.0.1

# Pydantic for data validation (included with FastAPI but explicit for clarity)
pydantic==2.5.0

# Additional dependencies that might be useful
python-multipart==0.0.6  # For form data handling if needed